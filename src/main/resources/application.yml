spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts.action: create
            create-target: create.sql

  datasource:
    url: jdbc:mysql://${MYSQL_HOST}:3306/DueTo-Database
    username: DueTo
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true
